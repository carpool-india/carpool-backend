{
  "installCommand": "cd .. && npm install",
  "buildCommand": "cd .. && npx turbo run build --filter=@rideshare/console",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
